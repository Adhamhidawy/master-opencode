---
phase: 02-interactive-learning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/challenge.ts
  - src/data/challenges.ts
  - src/components/interactive/challenge-card.tsx
  - src/app/challenges/page.tsx
autonomous: true
requirements:
  - INTR-05
  - CONT-02
  - PAGE-04

must_haves:
  truths:
    - "User can see all 6 scenario-based challenges on /challenges"
    - "User can select an answer for each challenge"
    - "Selected answer shows correct/incorrect visual feedback"
    - "Each challenge displays an explanation after answering"
    - "All 6 challenges display without errors after page load"
  artifacts:
    - path: "src/types/challenge.ts"
      provides: "Challenge type definition"
      contains: "export interface Challenge"
    - path: "src/data/challenges.ts"
      provides: "6 challenge objects migrated from index.html"
      min_lines: 40
    - path: "src/components/interactive/challenge-card.tsx"
      provides: "Client component for interactive challenge card with answer selection"
      contains: '"use client"'
    - path: "src/app/challenges/page.tsx"
      provides: "Challenges page route"
      contains: "export default"
  key_links:
    - from: "src/app/challenges/page.tsx"
      to: "src/data/challenges.ts"
      via: "import { challenges } from '@/data/challenges'"
      pattern: "import.*challenges"
    - from: "src/app/challenges/page.tsx"
      to: "src/components/interactive/challenge-card.tsx"
      via: "ChallengeCard component import"
      pattern: "import.*ChallengeCard"
    - from: "src/components/interactive/challenge-card.tsx"
      to: "src/types/challenge.ts"
      via: "Challenge type import"
      pattern: "import.*Challenge"
---

<objective>
Build the Challenges page (/challenges) with 6 interactive scenario-based challenges.

Purpose: Users test their OpenCode knowledge by selecting answers to real-world scenarios and receiving instant feedback with explanations. Per the user's Phase 2 feature list (feature #1, highest priority).

Output: Fully interactive /challenges page with client-side answer validation, correct/incorrect states, and feedback explanations. No backend needed.
</objective>

<execution_context>
@/Users/user1/Projects/master-opencode/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/user1/Projects/master-opencode/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@index.html
@src/types/lesson.ts
@src/data/cheat-sheet.ts
@src/app/globals.css
@src/lib/utils.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/types/lesson.ts:
```typescript
export interface Lesson {
  slug: string;
  title: string;
  description: string;
  chapter: number;
  difficulty: "beginner" | "intermediate" | "advanced";
  label: string;
  sections: LessonSection[];
}
```

From src/types/cheat-sheet.ts:
```typescript
export interface CheatSheetCategory {
  id: string;
  title: string;
  icon: string;
  iconBg: string;
  iconColor: string;
  rows: { key: string; description: string }[];
}
```

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```

From index.html lines 629-636 (challenge data source):
```javascript
const challengesData=[
  {title:"Add a Feature", scenario:"...", opts:["...","...","...","..."], ans:1, feedback:"Plan first, then Build..."},
  {title:"Debug an Error", scenario:"...", opts:["...","...","...","..."], ans:1, feedback:"Specific error + file reference..."},
  {title:"Quick One-Shot", scenario:"...", opts:["...","...","...","..."], ans:1, feedback:"opencode run lets you..."},
  {title:"Read-Only Review", scenario:"...", opts:["...","...","...","..."], ans:1, feedback:"Plan mode is read-only..."},
  {title:"Custom Workflow", scenario:"...", opts:["...","...","...","..."], ans:1, feedback:"Custom commands are reusable..."},
  {title:"Control Permissions", scenario:"...", opts:["...","...","...","..."], ans:1, feedback:"Bash permissions support..."}
];
```

Challenge card behavior from index.html lines 784-799:
- Each card shows title with 🎯 emoji, scenario text, 4 answer option buttons
- Clicking an answer: highlights correct in green, wrong in red, disables all buttons
- Shows feedback text: "Correct!" or "Not quite." + explanation
- Once answered, cannot re-answer

Tailwind theme tokens from globals.css:
- Colors: bg-bg, bg-bg-2, bg-bg-3, bg-border, text-text, text-text-2, text-text-3, bg-green, text-green-2, bg-red, text-red, bg-accent, text-accent-2
- Radius: rounded-sm (8px), rounded-lg (16px)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create challenge types and data file</name>
  <files>src/types/challenge.ts, src/data/challenges.ts</files>
  <read_first>
    - index.html (lines 629-636 for challenge data to migrate)
    - src/types/lesson.ts (type definition pattern to follow)
    - src/data/cheat-sheet.ts (data file pattern to follow)
  </read_first>
  <action>
    1. Create `src/types/challenge.ts` with a `Challenge` interface:
       ```typescript
       export interface Challenge {
         id: string;
         title: string;
         scenario: string;
         options: string[];
         correctIndex: number;
         feedback: string;
       }
       ```
       Export as named export. Follow the same pattern as `src/types/lesson.ts` (named interface export, no default).

    2. Create `src/data/challenges.ts` with a `challenges` array containing all 6 challenges migrated verbatim from index.html lines 629-636. Use the `Challenge` type. Each challenge gets a kebab-case `id` derived from the title (e.g., "Add a Feature" → "add-a-feature", "Debug an Error" → "debug-an-error", "Quick One-Shot" → "quick-one-shot", "Read-Only Review" → "read-only-review", "Custom Workflow" → "custom-workflow", "Control Permissions" → "control-permissions"). Map `opts` → `options`, `ans` → `correctIndex`, keep `scenario`, `feedback`, and `title` exactly as in the HTML.

    3. Import the `Challenge` type: `import type { Challenge } from "@/types/challenge";`
    4. Export as `export const challenges: Challenge[] = [...]`
  </action>
  <acceptance_criteria>
    - `src/types/challenge.ts` contains `export interface Challenge` with fields: id, title, scenario, options, correctIndex, feedback
    - `src/data/challenges.ts` contains `export const challenges: Challenge[]` with exactly 6 entries
    - `challenges[0].id` is `"add-a-feature"`
    - `challenges[0].correctIndex` is `1`
    - `challenges[5].title` is `"Control Permissions"`
    - `npx tsc --noEmit` passes with zero errors
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Challenge type and data file exist with all 6 challenges migrated from index.html, TypeScript compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Create ChallengeCard component and /challenges page</name>
  <files>src/components/interactive/challenge-card.tsx, src/app/challenges/page.tsx</files>
  <read_first>
    - src/types/challenge.ts (Challenge interface)
    - src/data/challenges.ts (challenge data)
    - src/app/globals.css (theme tokens for styling)
    - src/lib/utils.ts (cn utility)
    - src/components/ui/workflow-card.tsx (client component pattern reference)
    - index.html (lines 243-255 for challenge CSS styles to replicate)
  </read_first>
  <action>
    1. Create `src/components/interactive/challenge-card.tsx` as a `"use client"` component:
       - Props: `{ challenge: Challenge }` (import Challenge from `@/types/challenge`)
       - Uses `useState` to track: `selectedAnswer: number | null` (initially null), `showResult: boolean` (initially false)
       - Render structure (matching prototype aesthetic with Tailwind tokens):
         - Title: `<h4>` with 🎯 emoji prefix, `text-[1rem] font-bold`
         - Scenario: `<div>` with `text-[.88rem] text-text-2 leading-relaxed`
         - Options: 4 buttons in a vertical stack (`flex flex-col gap-1.5`)
           - Each button: `border border-border rounded-sm px-4 py-3 text-[.84rem] text-text-2 font-mono text-left cursor-pointer hover:border-accent hover:text-text transition-all`
           - When answered:
             - Correct answer button: add classes `border-green bg-green/10 text-green-2` (replace border/text classes)
             - Wrong selected answer: add classes `border-red bg-red/10 text-red`
             - All buttons: add `pointer-events-none opacity-60` (except correct which stays full opacity)
         - Feedback section: appears only when `showResult` is true
           - Background: `bg-green/10 text-green-2` for correct, `bg-red/10 text-red` for incorrect
           - Text: "Correct! " + feedback for correct, "Not quite. " + feedback for incorrect
           - Container: `mt-3 p-3 rounded-sm text-[.84rem]`
       - On option click: set `selectedAnswer` to index, set `showResult` to true

    2. Create `src/app/challenges/page.tsx` as a Server Component (NO "use client"):
       - Import `challenges` from `@/data/challenges`
       - Import `ChallengeCard` from `@/components/interactive/challenge-card`
       - Page metadata: title "Challenges — Master OpenCode"
       - Layout matches existing pages: centered container `max-w-[700px] mx-auto`, with `px-6 py-20 pt-28` (pt-28 for fixed navbar)
       - Section header:
         - Label: `text-[.75rem] font-bold uppercase tracking-[.15em] text-accent-2` → "Test Your Skills"
         - Title: `text-[clamp(1.8rem,4vw,2.8rem)] font-extrabold tracking-[-.02em]` → "Challenges"
         - Subtitle: `text-text-2` → "Real-world scenarios to test your OpenCode knowledge. Choose the best approach."
       - Map over `challenges` array rendering `<ChallengeCard challenge={challenge} />`
       - Add `key={challenge.id}` to each ChallengeCard

    3. Do NOT create the `src/components/interactive/` directory manually — it will be created when writing the file.
  </action>
  <acceptance_criteria>
    - `src/components/interactive/challenge-card.tsx` starts with `"use client"`
    - `src/components/interactive/challenge-card.tsx` imports `Challenge` from `@/types/challenge`
    - `src/components/interactive/challenge-card.tsx` uses `useState` for answer tracking
    - `src/app/challenges/page.tsx` does NOT have `"use client"` (server component)
    - `src/app/challenges/page.tsx` imports `challenges` from `@/data/challenges`
    - `src/app/challenges/page.tsx` imports `ChallengeCard` from `@/components/interactive/challenge-card`
    - `src/app/challenges/page.tsx` maps over challenges array with `.map()`
    - `npm run build` succeeds with no errors
    - Navigate to /challenges: page renders all 6 challenge cards with titles, scenarios, and clickable options
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>/challenges page renders 6 interactive scenario cards. Clicking an answer shows correct/incorrect feedback with explanation. All options become disabled after answering. TypeScript compiles and Next.js build succeeds.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with no errors
2. All 6 challenges render on /challenges with titles, scenarios, and 4 options each
3. Clicking an answer shows green for correct, red for incorrect
4. Feedback text appears below each answered challenge
5. No console errors on page load or interaction
</verification>

<success_criteria>
- /challenges page accessible and rendering all 6 challenges from structured TypeScript data
- Client-side answer selection with immediate visual feedback (green/red states)
- Explanation text shown after each answer
- Server Component page wraps Client Component cards (correct RSC/Client boundary)
- Clean build with zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-interactive-learning/02-01-SUMMARY.md`
</output>
